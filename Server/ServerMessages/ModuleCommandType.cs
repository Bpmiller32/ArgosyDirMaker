namespace Server.ServerMessages;

// Defines the command types that can be sent to modules
public enum ModuleCommandType
{
    // Command to start a module's operation
    Start,

    // Command to stop a module's operation
    Stop
}
