namespace Server.DataObjects;

// Represents a Parascript data file with metadata
// Currently doesn't add any additional properties beyond the base class
public class ParaFile : BaseFile
{
    // Inherits all properties from BaseFile
}
